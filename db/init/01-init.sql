-- supabase/postgres already ships pgcrypto, pgsodium, pgvector, pg_graphql,
-- pgjwt, pg_stat_statements, plpgsql. This file is for app-specific extensions
-- or bootstrap data only; leave empty if you don't need anything beyond the
-- Supabase baseline.

-- Example (uncomment as needed):
-- CREATE EXTENSION IF NOT EXISTS citext;
